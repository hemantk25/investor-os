# Real portfolio data — NEVER commit (repo is public)
report_data/
*.xlsx
*.xls
*.csv

# Local working files
.firecrawl/
.venv/
.pytest_cache/
__pycache__/
.env
memory.db
output/

# Instance data (real financials live here locally — never commit)
/data/
/briefs/
/profile/
!tests/fixtures/*.xlsx
